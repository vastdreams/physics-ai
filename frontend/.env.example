# Beyond Frontier — Frontend Environment Variables
# Copy to .env and customize as needed.
# Vite only exposes vars prefixed with VITE_.

# Backend API URL (leave empty in production when behind reverse proxy)
VITE_API_URL=http://localhost:5002

# WebSocket URL (defaults to VITE_API_URL if unset)
# VITE_WS_URL=http://localhost:5002
